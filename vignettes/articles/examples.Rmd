---
title: "Examples"
---

Each example below opens in a live [Shinylive](https://shinylive.io/) editor where you can read the source, modify it, and see the result — all in the browser.

<div class="examples-list" style="margin-top: 1.5rem;">

### [Old Faithful](../apps/old-faithful/index.html?_shinylive-mode=editor-terminal-viewer){target="_blank"}

The classic Shiny demo, rebuilt with nacre. A controlled slider drives a histogram of Old Faithful eruption wait times.

### [Shiny Modules](../apps/module/index.html?_shinylive-mode=editor-terminal-viewer){target="_blank"}

nacre components compose naturally with standard Shiny modules. This example embeds two independent counter modules side by side using `nacreOutput()` and `renderNacre()`.

### [Temperature Converter](../apps/temperature/index.html?_shinylive-mode=editor-terminal-viewer){target="_blank"}

Two inputs representing the same value in different units, where editing either one updates the other. Demonstrates how controlled inputs and `reactive()` keep multiple views of the same state in sync.

### [Todo List](../apps/todo/index.html?_shinylive-mode=editor-terminal-viewer){target="_blank"}

A TodoMVC-style app where items are stored as a `reactiveVal` holding a plain R list. Adding, toggling, filtering, and removing items are ordinary functions that update that list.

### [Optimistic Updates](../apps/optimistic-updates/index.html?_shinylive-mode=editor-terminal-viewer){target="_blank"}

nacre applies controlled input changes immediately in the browser before the server round-trip completes. If the server responds with a different value, the browser reconciles automatically.

</div>
