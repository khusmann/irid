% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event.R
\name{merge.irid_wire}
\alias{merge.irid_wire}
\title{Overlay one \code{irid_wire} over another}
\usage{
\method{merge}{irid_wire}(x, y, ...)
}
\arguments{
\item{x}{The default \code{irid_wire}.}

\item{y}{The override: an \code{irid_wire}, a bare callable, or \code{NULL}.}

\item{...}{Unused.}
}
\value{
An \code{irid_wire}.
}
\description{
Override-wins overlay used where a widget wrapper layers a caller's input
over its own default config. Each field of \code{y} (\code{subject}, \code{timing},
\code{coalesce}, \code{dom_opts}) wins when non-\code{NULL}; otherwise \code{x}'s field
carries through. \code{y} may be \code{NULL} (identity) or a bare callable (fills in
only the subject), both normalized first.
}
\details{
Extends the base \code{\link[=merge]{merge()}} generic rather than introducing a new one.
}
