% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotly.R
\name{PlotlyOutput}
\alias{PlotlyOutput}
\title{Reactive plotly output}
\usage{
PlotlyOutput(
  spec,
  ...,
  onClick = NULL,
  onHover = NULL,
  onUnhover = NULL,
  onDoubleclick = NULL,
  onDeselect = NULL,
  onSelecting = NULL,
  onBrushing = NULL,
  onLegendClick = NULL,
  onLegendDoubleclick = NULL,
  onClickAnnotation = NULL,
  onSunburstClick = NULL,
  onRelayout = NULL,
  container = NULL
)
}
\arguments{
\item{spec}{A zero-argument function returning a plotly object (from
\code{plotly::plot_ly()} or \code{plotly::ggplotly()}). Re-evaluated reactively;
\code{Plotly.react()} diffs the result client-side.}

\item{...}{Named state arguments, each a callable (\code{reactiveVal}, store
leaf, \code{reactiveProxy}) or constant. Recognized: \code{xaxis_range},
\code{yaxis_range}, \verb{xaxis<n>_range}, \verb{yaxis<n>_range}, \code{dragmode},
\code{hovermode}, \code{selected_ids}, \code{trace_visibility}. \code{NULL} means "don't
override the spec". Unknown names error — use \code{onRelayout} for fields
outside the table.

\code{selected_ids} is the box/lasso selection, keyed on plotly's per-point
\code{ids} (a character vector of id values). Binding it requires the plot to
supply \code{ids} (\code{plot_ly(ids = ~key)} / \code{aes(ids = key)}); because the value
is keyed on identity rather than position, the selection survives data
changes that renumber points (filtering, sorting, trace recomposition).}

\item{onClick, onHover, onUnhover, onDoubleclick}{Discrete pointer callbacks.
Each receives the (slimmed) plotly event payload.}

\item{onDeselect, onSelecting, onBrushing}{Selection-lifecycle notifications.
\code{onDeselect} is a side-effect notification only — when \code{selected_ids}
is bound, the clear already flows through its prop channel.}

\item{onLegendClick, onLegendDoubleclick, onClickAnnotation, onSunburstClick}{Discrete interaction callbacks.}

\item{onRelayout}{Escape hatch — receives the raw \code{plotly_relayout} payload
(flat dot-notation keys) for fields outside the translation table.}

\item{container}{Optional \code{shiny.tag} for the wrapper element.}
}
\value{
An \code{irid_widget} construct.
}
\description{
A first-class output primitive for interactive plotly charts, on par with
\link{PlotOutput} and \link{TableOutput}. Unlike the \code{{plotly}} htmlwidget — which
destroys and recreates the chart on every reactive update — \code{PlotlyOutput}
uses \code{Plotly.react()} for incremental updates, so a data change preserves
the user's zoom, pan, and selection (via plotly's \code{uirevision}).
}
\details{
\code{PlotlyOutput} is a thin wrapper over \link{IridWidget}. User-controllable UI
state (axis ranges, drag mode, selection, trace visibility) is exposed as
\strong{named reactive arguments}, each a two-way prop: bind a \code{reactiveVal},
store leaf, or \link{reactiveProxy} and the user's interaction writes back to it.
A \code{reactiveProxy} that rejects a write snaps the plot back. Discrete events
(clicks, hovers, legend interactions) are plain \verb{on*} callbacks.
}
