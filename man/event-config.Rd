% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event.R
\name{event-config}
\alias{event-config}
\alias{event_immediate}
\alias{event_throttle}
\alias{event_debounce}
\title{Event timing and transport config}
\usage{
event_immediate(coalesce = FALSE)

event_throttle(ms, leading = TRUE, coalesce = TRUE)

event_debounce(ms, coalesce = TRUE)
}
\arguments{
\item{coalesce}{If \code{TRUE}, gate on server idle so events never queue
faster than the server can process them. Defaults to \code{FALSE} for
\code{event_immediate()} and \code{TRUE} for \code{event_throttle()}/\code{event_debounce()}.}

\item{ms}{Minimum interval (throttle) or quiet period (debounce) in
milliseconds.}

\item{leading}{If \code{TRUE} (default), fire immediately on the first
event. If \code{FALSE}, wait for the timer before firing.}
}
\value{
An \code{irid_event_config} struct.
}
\description{
Configure how event callbacks are dispatched from the browser to the
server. Pass the result to an element's \code{.event} prop to control timing
for \strong{all} events on that element (auto-bind synthetic write-back plus
any explicit \verb{on*} handlers).
}
\details{
\itemize{
\item \code{event_immediate()}: Fires on every event with no rate limiting.
\item \code{event_throttle()}: Fires at most every \code{ms} milliseconds while the
event is active.
\item \code{event_debounce()}: Waits until the user pauses for \code{ms} milliseconds
before firing.
}

When \code{.event} is omitted, irid applies a per-event default:
\code{event_debounce(200)} for auto-bind synthetic events,
\code{event_immediate()} for everything else.

\code{.event} accepts either a single config struct (applies to every event
on the element) or a named list keyed by lowercase DOM event name
(\code{input}, \code{change}, \code{keydown}, ...) for per-event overrides. Events
not covered by the list fall back to the per-event default.

Use the element-level \code{.prevent_default = TRUE} prop to call
\code{event.preventDefault()} in the browser before dispatch.
}
\section{The event object}{

The \code{event} argument passed to handlers is a list containing all
primitive-valued properties (string, numeric, logical) from the browser
event object, plus these element properties:
\describe{
\item{\code{value}}{The element's current value (character).}
\item{\code{valueAsNumber}}{Numeric value of the element, or \code{NA} if the
input is empty or non-numeric (e.g. a blank text box). Useful for
range and number inputs.}
\item{\code{checked}}{Logical, for checkbox and radio inputs.}
}
Keyboard events additionally include \code{key}, \code{code}, \code{ctrlKey},
\code{shiftKey}, \code{altKey}, and \code{metaKey}.
}

