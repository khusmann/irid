% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{reactiveStore}
\alias{reactiveStore}
\title{Hierarchical reactive state container}
\usage{
reactiveStore(initial)
}
\arguments{
\item{initial}{A bare named list describing the initial shape.
Sub-positions classify as follows: bare named lists (length > 0)
become store sub-nodes; anything else (scalars, vectors, \code{NULL},
unnamed/empty bare lists, classed lists, or any value wrapped in
\code{I()}) becomes a leaf. Partially-named bare lists are an error.}
}
\value{
A callable \code{reactiveStore} node with class
\code{c("reactiveStore", "reactive", "function")}. Sub-trees share the
same class. Leaf positions are plain \code{shiny::reactiveVal()}s with
class \code{c("reactiveVal", "reactive", "function")} — the shared
\code{"reactive"} class is what auto-bind dispatches on.
}
\description{
Builds a callable hierarchical state tree from a nested list. The shape
rule is simple: a bare named list (length > 0) becomes a navigable
\code{reactiveStore} node (every sub-tree is itself a \code{reactiveStore});
everything else becomes a plain \code{\link[shiny:reactiveVal]{shiny::reactiveVal()}} leaf that accepts
any value on write. To force a bare named list to be treated as a leaf
instead of a store, wrap it in \code{\link[base:AsIs]{base::I()}}.
}
\details{
Every node is callable: \code{node()} reads, \code{node(value)} writes. Leaves
replace; store nodes patch (only the keys present in the patch are
updated). Unknown keys on a store-node patch are an error. Types are
not enforced.

\code{length()} on a leaf returns \code{1} (the underlying \code{reactiveVal} is a
callable, and neither R's closure default nor shiny override
\code{length}). htmltools' \code{dropNullsOrEmpty} calls \code{length()} on every
attribute value, so this is what lets \code{value = state$leaf} survive
tag construction. Use \code{length(leaf())} for the underlying length.
}
