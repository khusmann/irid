% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irid_widget.R
\name{IridWidget}
\alias{IridWidget}
\title{Create a widget node for use in irid tag trees}
\usage{
IridWidget(
  dep,
  container,
  ...,
  .config = list(),
  .event = NULL,
  .render = NULL,
  .widget_name = NULL
)
}
\arguments{
\item{dep}{An \code{\link[htmltools:htmlDependency]{htmltools::htmlDependency()}} for the widget's JS/CSS.}

\item{container}{A \link[shiny:reexports]{shiny::tags} element that serves as the widget's
DOM container. The element is assigned the widget's auto-generated
ID and the \code{irid-widget} CSS class.}

\item{...}{Named arguments: reactive functions become data channels,
\verb{on*} functions become event handlers, and all other values become
static init-time config.}

\item{.config}{A named list of static configuration values. Merged with
non-reactive, non-event \code{...} args (\code{.config} values take precedence).}

\item{.event}{An \link{irid_event_config} or named list of them, applied to
every event on the widget (same semantics as the element-level
\code{.event} prop on regular tags).}

\item{.render}{Optional string naming the render channel. Channel
messages for this field carry \code{isRender: true}. Default \code{NULL}.}

\item{.widget_name}{Optional string to override the auto-derived widget
name (from \code{dep$name} with hyphens/underscores stripped). Passed to
the client as \code{msg.widget} in the init message.}
}
\value{
An object of class \code{"irid_widget"}.
}
\description{
\code{IridWidget()} is a low-level constructor for package authors to wrap
JavaScript libraries (like CodeMirror, Leaflet, or Plotly) as irid
components. It returns a first-class irid node that \code{\link[=process_tags]{process_tags()}}
and \code{\link[=irid_mount_processed]{irid_mount_processed()}} handle alongside \code{Each}/\code{When}/\code{Match}.
}
\details{
Named \code{...} arguments are split into three categories:
\itemize{
\item Names matching \verb{^on[A-Z]} become event handlers (same as tag \verb{on*}
attrs), with timing configured by \code{.event}.
\item Reactive-valued functions (see \code{\link[=is_irid_reactive]{is_irid_reactive()}}) become data
channels: their values are observed and pushed to the client on
change via \code{irid-widget-channel} messages.
\item All other named values become static init-time config, merged with
\code{.config} (\code{.config} values win on name collision).
}

The \code{.render} argument names the channel that triggers a full re-render
on the client — channel messages for this field carry \code{isRender: true}.

End-users never call \code{IridWidget()} directly. Package authors create
component functions like:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{CodeMirror <- function(content, mode = "javascript",
                       onChange = NULL, onCursorActivity = NULL) \{
  IridWidget(
    dep = codemirror_dep(),
    container = tags$div(style = "height: 300px;"),
    content = content,
    .config = list(mode = mode),
    onChange = onChange,
    onCursorActivity = onCursorActivity
  )
\}
}\if{html}{\out{</div>}}
}
