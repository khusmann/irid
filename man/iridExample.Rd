% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example.R
\name{iridExample}
\alias{iridExample}
\title{Run a irid example application}
\usage{
iridExample(
  example = NA,
  port = getOption("shiny.port"),
  launch.browser = getOption("shiny.launch.browser", interactive()),
  host = getOption("shiny.host", "127.0.0.1"),
  display.mode = c("showcase", "normal", "auto")
)
}
\arguments{
\item{example}{Name of the example to run (e.g. \code{"todo"}, \code{"old-faithful"})
— the same name used in its website URL. If omitted, the available
examples are listed.}

\item{port}{The TCP port the application should listen on. Defaults to
\code{getOption("shiny.port")}, or a random port if unset.}

\item{launch.browser}{If \code{TRUE}, the system's default web browser is
launched automatically. Defaults to \code{TRUE} in interactive sessions.}

\item{host}{The IPv4 address the application should listen on. Defaults to
\code{getOption("shiny.host", "127.0.0.1")}.}

\item{display.mode}{The display mode. Defaults to \code{"showcase"} — the
annotated source shown alongside the running app; use \code{"normal"} to run
the app by itself, or \code{"auto"} to defer to the app's own setting.}
}
\value{
If \code{example} is supplied, launches the app and blocks until it is
stopped. Otherwise returns the available example names invisibly as a
character vector (after listing them).
}
\description{
Launches one of the example apps shipped with the package, mirroring
\code{\link[shiny:runExample]{shiny::runExample()}}. The examples are the same apps published as editable
editors on the package website.
}
\details{
Called with no argument (or an unrecognised name), it lists the available
examples instead of launching one.

Some examples depend on packages beyond irid's hard dependencies (e.g.
bslib, plotly). The runner checks for these up front and, if any are
missing, stops with a message naming what to install. A couple of examples
(\code{codemirror}, \code{plotly}) also load JavaScript from a CDN at runtime, so they
need an internet connection even when run locally.

By default the app runs in Shiny's \emph{showcase} display mode, so the annotated
source appears beside the running app (as with \code{\link[shiny:runExample]{shiny::runExample()}}). Pass
\code{display.mode = "normal"} to run the app on its own.

The remaining arguments mirror \code{\link[shiny:runExample]{shiny::runExample()}}.
}
\examples{
# List the available examples
iridExample()

if (interactive()) {
  iridExample("todo")
}
}
