% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_tags.R
\name{resolve_render_hooks}
\alias{resolve_render_hooks}
\title{Materialize a node's own htmltools render hooks / tag function}
\usage{
resolve_render_hooks(node)
}
\arguments{
\item{node}{A node from the tag tree.}
}
\value{
The node with its top-level render hooks / tag function resolved.
}
\description{
bslib (and htmltools generally) defer structure to \strong{render time}: a
\code{shiny.tag} can carry \code{.renderHooks} that build its final DOM
(\code{layout_sidebar()}'s grid wrapper, \code{card()}'s fill plumbing, ...), and
a \code{shiny.tag.function} produces its tags only when called. \code{\link[=process_tags]{process_tags()}}
rebuilds each tag from \code{name}/\code{attribs}/\code{children}, so without running
these first the deferred wrapper is silently dropped (see GH #27).
}
\details{
This runs only the node's \emph{own} hooks (one level), looping until the node
is a resolved tag / non-tag. Unlike \code{\link[htmltools:as.tags]{htmltools::as.tags()}} it does \strong{not}
recurse into children — \code{process_tags}' walker descends into them itself
and resolves each child's hooks as it arrives. That separation is what
lets irid's own children (reactive functions, \code{irid_output},
\code{irid_widget}, control-flow nodes) survive: they never carry render hooks,
so they pass straight through, while a child hook that \emph{moved} them into a
new wrapper still gets walked.
}
\keyword{internal}
