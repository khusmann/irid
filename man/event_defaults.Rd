% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/widget.R
\name{event_defaults}
\alias{event_defaults}
\title{Layer wrapper-default event-timing config under a caller's \code{.event}}
\usage{
event_defaults(user, ...)
}
\arguments{
\item{user}{The \code{.event} value the caller passed (often \code{NULL}).}

\item{...}{Named \code{irid_event_config} entries (wrapper defaults).}
}
\value{
The merged \code{.event} value, suitable to pass to \code{\link[=IridWidget]{IridWidget()}}.
}
\description{
Three-tier resolution for an element-level \code{.event} prop:
\enumerate{
\item Caller's \code{.event} (highest) — a scalar \code{irid_event_config} wins
everywhere; a named list wins per event.
\item Wrapper defaults (middle) — the \code{...} entries supply per-event
defaults the wrapper author thinks are sensible for the library
(e.g. \code{change = event_debounce(200)} for an editor).
\item Framework default (lowest) — applied by \code{\link[=process_tags]{process_tags()}} for any
event not covered by the layered result.
}
}
\details{
Generic — plain-tag wrappers can use this too. A \code{SearchInput}
wrapper might write \code{event_defaults(.event, input = event_debounce(500))}
to make debounced input the default for its callers.
}
